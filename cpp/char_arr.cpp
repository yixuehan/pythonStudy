#include <iostream>
#include <fstream>
using namespace std;

#include <boost/timer/timer.hpp>
using namespace boost;

enum color{a,b,c};

int main()
{
    // ifstream ifs("in.txt");
    // char ch[20];
    // ifs >> ch;
    cout << sizeof(color);
}
